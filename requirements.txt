# Lab 24 — Eval + Guardrail stack. Verified on Python 3.11.
# (Heavy ML wheels — ragas/presidio/spacy — are not available for 3.14 yet; use 3.11.)

# ── Core RAG + eval ──────────────────────────────────────────────────────────
openai>=1.40,<2
ragas==0.2.14
datasets
# ragas 0.2.x needs the langchain 0.2.x line (0.3+ removed chat_models.vertexai):
langchain==0.2.17
langchain-core==0.2.43
langchain-community==0.2.19
langchain-openai==0.1.25
langchain-text-splitters==0.2.4

# ── LLM-as-Judge calibration + charts ────────────────────────────────────────
scikit-learn>=1.4
pandas
matplotlib

# ── Guardrails: PII ──────────────────────────────────────────────────────────
presidio-analyzer>=2.2
presidio-anonymizer>=2.2
spacy>=3.7            # after install: python -m spacy download en_core_web_lg

# ── Output guard / injection: Groq safety models via OpenAI-compatible API ────
# (uses the `openai` client above with base_url=https://api.groq.com/openai/v1)
#   - meta-llama/llama-prompt-guard-2-86m   (injection / jailbreak)
#   - openai/gpt-oss-safeguard-20b          (output content safety)

# ── Day 18 RAG pipeline (optional, for the original modules) ──────────────────
pypdf>=4.0
python-dotenv>=1.0
